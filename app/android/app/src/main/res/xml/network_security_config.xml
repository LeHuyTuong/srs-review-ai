<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9 (API 28) and later block cleartext HTTP by default, so a request to
  http://10.0.2.2:8000 is rejected by the OS before it ever reaches dio. This app
  deliberately talks to the FastAPI review proxy over plain HTTP in development:

    10.0.2.2   the host machine as seen from the Android emulator
    localhost  a proxy reached through "adb reverse tcp:8000 tcp:8000"
    127.0.0.1  same, when the app resolves it literally

  Rather than opening cleartext globally (which would weaken every other host
  the app might ever reach), only these dev hosts are exempted. The base-config
  keeps every other destination HTTPS-only.

  Running on a PHYSICAL device against a laptop on the same WiFi? Add that LAN
  address here as well (a domain entry for 192.168.1.20, say) and point the app
  at it at build time with the dart-define API_BASE_URL=http://192.168.1.20:8000

  See docs/architecture-review-mobile-2026-09-11.md (P0-3).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
